# Input number then round to 2 places

response = input("Enter a number: ")
# print(f"{response} rounded to 2 decimal places is {round(response,2)}")
print(f'{response} rounded to 2 decimal places is {round(float(response), 2)}')